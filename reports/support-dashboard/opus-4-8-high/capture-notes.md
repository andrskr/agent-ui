# Browser checks

Batch: `1789736285716-1927fb31-2496-460c-bc6e-3bcbe0fe96ce`.

The three screenshots show the initial This week state at 1440 × 1000 CSS pixels and scale 2. The
generated source was not changed during capture.

All three dev pages loaded. Switching to Last week changed the cards to 84 tickets created, 78
resolved, and 3.1 hours. Dates, bar values, category counts, and percentages also changed. The bar
scale stayed fixed. Each bar tooltip showed the day and count. The initial state was restored before
capture.

The context page has a display error: its donut total is outside the center and clipped near the
chart's upper-left edge. The baseline and repair pages show their donut totals in the center.

All three runs passed automated checks. These browser observations do not change the saved
verification results and are not a visual quality score. All capture servers were stopped.
