# Capture notes

Batch: `1789744503179-70fc33d9-ba50-4967-8533-e9bd76e69672`.

The two PNGs show the initial page at 1440 × 1000 CSS pixels and scale 2. No controls were clicked
and no interaction tests were performed.

The original app directories had been replaced by the Sonnet batch before capture. The Opus source
was recovered from the ledger's saved Write and Edit events in separate temporary app copies. Source
formatting was reapplied. Every source file and the build configuration, package manifest, and
lockfile matched the SHA-256 hashes in the original run report. The recovered apps used the
corresponding Sonnet setup dependencies, with matching package and lockfile hashes. The copies were
built locally and captured from production previews. No new model run was made. The stored metrics
were not changed.

The saved images were opened to check their content. All capture servers were stopped.
