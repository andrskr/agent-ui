interface:
  display_name: 'Experiment report'
  short_description: 'Prepare experiments, run models, and save reports'
  default_prompt: 'Use $experiment-report for the selected scenario and model. Follow the current experiment setup. Generate the standard HTML report and capture UI screenshots when requested. Stop all servers started for the work. Preparation and report-only requests must not start a provider.'
